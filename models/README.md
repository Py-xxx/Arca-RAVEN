# Models

Saved RL policies and ONNX exports.

## Structure
- `checkpoints/` — PyTorch checkpoint files during training
- `exported/` — ONNX models ready for TensorRT conversion
- `deployed/` — TensorRT engine files for Jetson AGX Orin
